# Production Requirements for Audio Digit Classifier
# Optimized for Railway deployment - ML models + VAD only

# Flask Core (pinned for faster resolution)
Flask==2.3.3
python-dotenv==1.0.0
gunicorn==21.2.0

# Core ML Dependencies (CPU-only torch for smaller size)
torch==2.1.0+cpu --find-links https://download.pytorch.org/whl/torch_stable.html
numpy==1.24.3
scikit-learn==1.3.0

# Audio Processing (Core)
librosa==0.10.1
soundfile==0.12.1
scipy==1.11.1

# VAD and Audio Utils
webrtcvad==2.0.10

# Remove unnecessary dependencies for Python 3.11
# pickle5 not needed for Python 3.8+
# pathlib2 not needed for Python 3.4+