# Production Requirements for Audio Digit Classifier
# Streamlined for Railway deployment - ML models + VAD only

# Flask Core
Flask>=2.3.0
python-dotenv>=0.19.0
gunicorn>=20.1.0

# Core ML Dependencies
torch>=1.12.0
numpy>=1.20.0
scikit-learn>=1.1.0

# Audio Processing (Core)
librosa>=0.10.0
soundfile>=0.10.0
scipy>=1.9.0

# VAD and Audio Utils
webrtcvad>=2.0.10

# Data handling for models
pickle5>=0.0.11; python_version < '3.8'

# System utilities
pathlib2>=2.3.0

# Optional but recommended for production
# collections-extended>=2.0.2