# Simple Railway deployment - no Docker, just direct Python hosting
[variables]
NIXPACKS_PYTHON_VERSION = "3.11"

[build]
cmd = "pip install --no-cache-dir -r requirements.txt"

[start]
cmd = "python app.py"
